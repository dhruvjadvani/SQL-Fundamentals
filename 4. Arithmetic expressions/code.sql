SELECT -2;
SELECT 20*15;
SELECT 3/5;
SELECT 18%4;
SELECT 30+234;
SELECT 3-5;

/* SQL supports the common rules of operator precedence.
In the list above operators are sorted by the decrease of priority. SQL also supports brackets to make an operator take priority over any other.*/
