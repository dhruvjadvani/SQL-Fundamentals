/* Let's consider an example of an SQL query that defines a table "census" with 5 columns: "id" of type INTEGER,
"name" of type VARCHAR(20), "birth_place_latitude" of type REAL, "year_income" of type DECIMAL(20,3), and "is_parent" of type BOOLEAN. */

CREATE TABLE census (
    id INTEGER,
    name VARCHAR(20),
    birth_place_latitude REAL,
    year_income DECIMAL(20,3),
    is_parent BOOLEAN
);
